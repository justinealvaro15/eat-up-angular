import { FcsSearchPipe } from './fcs-search.pipe';

describe('FcsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FcsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
